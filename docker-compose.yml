version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
    volumes:
      - aegis_data:/app

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"

volumes:
  aegis_data:
